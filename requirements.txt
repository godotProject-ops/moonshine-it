# Core dependencies for Moonshine fine-tuning
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0

# Training & Evaluation
evaluate>=0.4.0
jiwer>=3.0.0
tensorboard>=2.14.0

# Audio processing
soundfile>=0.12.0
torchaudio>=2.0.0

# Data handling
pyyaml>=6.0
tqdm>=4.66.0
numpy>=1.24.0
pandas>=2.0.0

# Optional: ONNX export and deployment
onnx>=1.14.0
onnxruntime>=1.16.0
optimum>=1.14.0  # For ONNX export and merged decoder

# Live transcription (optional)
# Install with: pip install -r requirements-live.txt
